// initializes the tooltip function

$(document).ready(function(){
    $('.tooltipped').tooltip();
  });
  
  
  
  // initalizes floating button
  
  $('.fixed-action-btn').floatingActionButton({
    toolbarEnabled: true
    
    
  });